# Migrations

SQL files, applied in order by filename. The user writes these — schema is
design.

Naming: `NNN_description.sql`, e.g., `001_initial.sql`, `002_add_clicks.sql`.

A separate `down` migration is good practice but not required for learning.
